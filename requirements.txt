Flask>=2.0
Flask-SQLAlchemy>=2.5
Werkzeug>=2.0 # Ensure compatibility for security features
python-dotenv>=0.19 # For managing environment variables like secret keys (optional but good practice)
psycopg2-binary # PostgreSQL driver
